import { ICouponService } from 'src/coupon/app/module';
import { Coupon } from 'src/coupon/domain';
import { CouponAccountDto, UpdateCouponDTO } from '../dto';
import { ICouponRepository } from 'src/coupon/domain/data.abstract';
import { IUserRepository } from 'user/domain';
import { IBetRepository } from 'src/bet/domain/data.abstract';
import { ICouponBetRepository } from 'src/couponBet/domain/data.abstract';
import { MatchGateway } from 'src/match/adapter/module/match.gateway';
import { IMatchRepository } from 'src/match/domain';
import { Connection } from 'typeorm';
import { UpdateMatchDTO } from 'src/match/adapter/dto';
export declare class CouponService implements ICouponService {
    private readonly connection;
    private couponsRepository;
    private userRepository;
    private betRepository;
    private cpRepository;
    private matchRepository;
    private readonly matchGateway;
    private readonly logger;
    constructor(connection: Connection, couponsRepository: ICouponRepository, userRepository: IUserRepository, betRepository: IBetRepository, cpRepository: ICouponBetRepository, matchRepository: IMatchRepository, matchGateway: MatchGateway);
    fetchAll(): Promise<Coupon[]>;
    fetchOne(id: string): Promise<Coupon>;
    search(data: Partial<Coupon>): Promise<Coupon>;
    add(data: CouponAccountDto): Promise<Coupon>;
    edit(data: UpdateCouponDTO): Promise<Coupon>;
    setState(id: string): Promise<boolean>;
    remove(id: string): Promise<boolean>;
    checkCoupons(data: UpdateMatchDTO): Promise<any>;
    private isBetWinning;
    private updateBetStatus;
    private getActiveCoupons;
    private updateCouponStatus;
    validatePendingCoupons(): Promise<void>;
    private canBetBeValidatedEarly;
}
