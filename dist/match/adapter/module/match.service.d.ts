import { IMatchService } from 'src/match/app/module';
import { IMatchRepository, Match } from 'src/match/domain';
import { MatchAccoutDTO, UpdateMatchDTO, UpdateMatchScoreEventDto, UpdateStateDto } from '../dto';
import { IArbitreRepository } from 'src/arbitre/domain';
import { ITeamRepository } from 'src/team/domain';
import { IPouleRepository } from 'src/poule/domain';
import { IPlayerRepository } from 'src/player/domain';
import { IMatchEventRepository } from 'src/matchEvents/domain';
import { MatchGateway } from './match.gateway';
export declare class MatchService implements IMatchService {
    private matchRepository;
    private arbitreRepository;
    private teamRepository;
    private pouleRepository;
    private playerRepository;
    private eventRepository;
    private readonly matchGateway;
    private readonly logger;
    constructor(matchRepository: IMatchRepository, arbitreRepository: IArbitreRepository, teamRepository: ITeamRepository, pouleRepository: IPouleRepository, playerRepository: IPlayerRepository, eventRepository: IMatchEventRepository, matchGateway: MatchGateway);
    fetchAll(): Promise<Match[]>;
    fetchOne(id: string): Promise<Match>;
    search(data: Partial<Match>): Promise<Match>;
    add(data: MatchAccoutDTO): Promise<Match>;
    private addFullImageUrls;
    edit(data: UpdateMatchDTO): Promise<Match>;
    setState(id: string): Promise<boolean>;
    remove(id: string): Promise<boolean>;
    updateScore(data: UpdateMatchScoreEventDto): Promise<Match>;
    updateState(data: UpdateStateDto): Promise<Match>;
}
